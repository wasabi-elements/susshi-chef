- breadcrumb :api_tokens, 'New'

= message_panel 'Note', type: :primary, icon: 'info-circle' do
  Once an API token has been created, it will be presented to you ONE TIME ONLY and could not be displayed or changed afterwards.

= panel(@title) do
  = render 'form'
