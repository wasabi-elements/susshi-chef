- breadcrumb @target_host

= attributes_panel "Details of '#{@target_host.name}'" do
  = row_key_value 'Partition', @target_host.partition.name, icon: 'fa-cube'
  = row_key_value 'Hostname', @target_host.name, icon: 'fa-globe'
